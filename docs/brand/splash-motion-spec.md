# RepoAtlas splash motion

Personality: precise, restrained, reliable.
Duration: 1200ms splash, then the verified static mark.
Timeline: 0-240ms fade in the verified raster mark, 240-840ms reveal the live Geist "RepoAtlas" wordmark left to right, 840-1200ms settle on the static brand lockup.
The wordmark is live text, not reconstructed letter paths. Reduced motion shows the final frame immediately. Pixel2Motion commit e9faedb informed the timing; the shipped asset is the existing mark plus CSS motion, not a runtime Python or Playwright dependency.
