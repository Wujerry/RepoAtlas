# Use complementary FTS5 indexes for local search

Local search combines token-oriented indexing for English and structured metadata with trigram indexing for Chinese text, path fragments, and partial names. A single default tokenizer does not serve both retrieval patterns well enough, while local merged ranking keeps natural-language asset lookup useful even when no AI provider is available.

