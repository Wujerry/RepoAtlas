# Never delete project directories

RepoAtlas removes managed records but never offers an operation that deletes a real project directory. The product controls knowledge about local code assets rather than owning those assets, so removing a Project or Scan Root must remain categorically separate from filesystem deletion and must preserve that distinction in confirmation copy.

